// Run the same real AI SDK + native Google protocol contract in workerd.
// Only provider HTTP is faked; no model keys, sandboxes, or external calls are used.
import "./model-gateway.test";
import "./model-gateway-service.test";
